import { DateFormatter } from "./helpers";

describte("Tests for DateFormatter", () => {
  test.todo("Outputs current day");
  test.todo("Outputs current time");
  test.todo("Outputs am and pm");
  test.todo("Outputs time in another timezone");
});
